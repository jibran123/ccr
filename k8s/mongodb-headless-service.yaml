apiVersion: v1
kind: Service
metadata:
  name: mongodb-headless
  labels:
    app: ccr
    component: mongodb
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: ccr
    component: mongodb
  ports:
  - name: mongodb
    protocol: TCP
    port: 27017
    targetPort: 27017
